#:kivy 2.3.0

# --- MÀU SẮC ---
#:set c_bg (0.05, 0.05, 0.1, 1)
#:set c_primary (0, 0.8, 1, 1)
#:set c_card (0.15, 0.15, 0.25, 1)
#:set c_btn_press (0, 0.5, 0.7, 1)

# --- STYLE NÚT BẤM ---
<ModernButton@Button>:
    background_normal: ''
    background_color: (0,0,0,0)
    canvas.before:
        Color:
            rgba: c_primary if self.state == 'normal' else c_btn_press
        RoundedRectangle:
            pos: self.pos
            size: self.size
            radius: [10,]
    bold: True
    color: (1,1,1,1)
    font_size: '24sp'
    # 👇 THÊM ĐOẠN NÀY ĐỂ TẠO VIỀN CHỮ (Giúp chữ nổi lên trên mọi nền):
    outline_color: (0, 0, 0, 0.5)  # Viền đen mờ
    outline_width: 2               # Độ dày viền

# --- STYLE CARD (Đã nâng cấp để có thể đổi màu) ---
<DashboardCard@ButtonBehavior+BoxLayout>:
    title: ''   
    desc: ''
    # THÊM DÒNG NÀY: Thiết lập giá trị mặc định là màu c_card cũ
    card_color: (0.15, 0.15, 0.25, 1) 
    
    orientation: 'vertical'
    padding: 20
    spacing: 10
    canvas.before:
        Color:  
            # SỬA DÒNG NÀY: Thay c_card bằng self.card_color
            rgba: self.card_color
        RoundedRectangle:
            pos: self.pos
            size: self.size
            radius: [15,]
            
    # Các phần Label bên dưới giữ nguyên không đổi...
    Label:
        text: root.title
        font_size: '22sp'
        bold: True
        color: c_primary
        size_hint_y: 0.3
    Label:
        text: root.desc
        font_size: '14sp'
        color: (0.7, 0.7, 0.7, 1)
        size_hint_y: 0.7
        halign: 'center'

# --- ĐỊNH NGHĨA ĐƯỜNG KẺ NGANG ---
<Separator@Widget>:
    canvas:
        Color:
            rgba: (0.3, 0.3, 0.3, 1) # Màu xám cho đường kẻ
        Rectangle:
            pos: self.pos
            size: self.size

ScreenManager:
    HomeScreen:
    SecurityScreen:
    TutorScreen:
    ElderlyScreen:
# ================= HOME SCREEN =================
<HomeScreen>:
    name: 'home'
    canvas.before:
        Color:
            rgba: c_bg
        Rectangle:
            pos: self.pos
            size: self.size
    
    # 1. LỚP CHÍNH: BONG BÓNG VÀ TRẠNG THÁI
    FloatLayout:
        # Bong bóng 3D nằm chính giữa
        Bubble3D:
            id: bubble_widget
            pos_hint: {'center_x': 0.5, 'center_y': 0.55}
            size_hint: (None, None)
            size: (200, 200)

        # Label hiển thị trạng thái giọng nói (nhỏ bên dưới bong bóng)
        Label:
            id: lbl_status
            text: "Đang lắng nghe..."
            font_size: '18sp'
            color: (0.6, 0.8, 1, 1)
            pos_hint: {'center_x': 0.5, 'y': 0.25}
            size_hint: (1, 0.1)
            halign: 'center'

        # Nút Mở Menu (Góc trên phải)
        Button:
            text: "MENU"
            font_size: '20sp'
            size_hint: (None, None)
            size: (100, 50)
            pos_hint: {'right': 0.95, 'top': 0.95}
            background_color: (0.2, 0.2, 0.3, 1)
            on_release: root.toggle_menu()

    # 2. LỚP PHỦ MENU (Overlay) - Mặc định ẩn (opacity: 0)
    BoxLayout:
        id: menu_overlay
        orientation: 'vertical'
        size_hint: (0.9, 0.8)
        pos_hint: {'center_x': 0.5, 'center_y': 0.5}
        padding: 20
        spacing: 20
        opacity: 0 
        disabled: True # Không bấm được khi ẩn
        
        canvas.before:
            Color:
                rgba: (0, 0, 0, 0.9) # Nền đen mờ che bong bóng
            RoundedRectangle:
                pos: self.pos
                size: self.size
                radius: [20,]

        Label:
            text: "BẢNG ĐIỀU KHIỂN THỦ CÔNG"
            font_size: '24sp'
            bold: True
            size_hint_y: 0.15
            color: c_primary

        GridLayout:
            cols: 2
            spacing: 15
            size_hint_y: 0.7

            DashboardCard:
                title: "AN NINH"
                desc: "Camera giám sát"
                card_color: (0.1, 0.2, 0.4, 1)
                on_release: app.root.current = 'security'

            DashboardCard:
                title: "HỌC TẬP"
                desc: "Gia sư Tiếng Anh"
                card_color: (0.25, 0.1, 0.35, 1)
                on_release: app.root.current = 'tutor'

            DashboardCard:
                title: "TÂM SỰ"
                desc: "Trò chuyện người cao tuổi"
                card_color: (0.05, 0.35, 0.35, 1)
                on_release: app.root.current = 'elderly'

            # Nút THOÁT ỨNG DỤNG (Theo yêu cầu: Chuyển về bong bóng)
            DashboardCard:
                title: "THOÁT RA"
                desc: "Đóng Menu, về Bong bóng"
                card_color: (0.4, 0.1, 0.1, 1)
                on_release: root.close_menu() 

        # Nút thoát app thật sự (nếu cần)
        Button:
            text: "Tắt nguồn hệ thống"
            size_hint_y: 0.1
            background_color: (1, 0, 0, 0.5)
            on_release: root.quit_app()

# ================= SECURITY SCREEN =================
<SecurityScreen>:
    name: 'security'
    canvas.before:
        Color:
            rgba: c_bg
        Rectangle:
            pos: self.pos
            size: self.size

    # Dùng FloatLayout để có thể lớp phủ Menu Cài đặt lên trên
    FloatLayout:
        BoxLayout:
            orientation: 'vertical'
            padding: 10
            spacing: 10
            pos_hint: {'x': 0, 'y': 0}
            size_hint: (1, 1)

            # HEADER
            BoxLayout:
                size_hint_y: 0.08
                Button:
                    text: "< HOME"
                    size_hint_x: 0.15
                    background_color: (0.1, 0.2, 0.4, 1)
                    on_release: app.root.current = 'home'
                
                Button:
                    text: "ĐỔI CAMERA"
                    size_hint_x: 0.2
                    background_color: (0, 0.6, 0.8, 1)
                    on_release: root.change_camera_source()
                    
                Button:
                    id: btn_cam_toggle
                    text: "TẮT CAMERA"
                    size_hint_x: 0.2
                    background_color: (1, 0, 0, 1)
                    on_release: root.toggle_camera() 

                Label:
                    text: "CAMERA MONITOR"
                    bold: True
                    color: c_primary

            # BODY
            BoxLayout:
                orientation: 'horizontal'
                spacing: 10

                # 1. CAMERA chính
                BoxLayout:
                    size_hint_x: 0.65
                    canvas.before:
                        Color:
                            rgba: (0.25, 0.1, 0.35, 1)
                        Rectangle:
                            pos: self.pos
                            size: self.size
                        Color:
                            rgba: c_primary
                        Line:
                            rectangle: (self.x, self.y, self.width, self.height)
                            width: 1.5
                    Image:
                        id: img_camera
                        allow_stretch: True
                        keep_ratio: True

                # 2. KHUNG CHAT VÀ NÚT CHÍNH
                BoxLayout:
                    orientation: 'vertical'
                    size_hint_x: 0.35
                    spacing: 10
                    padding: 5
                    
                    ScrollView:
                        size_hint_y: 0.6
                        canvas.before:
                            Color:
                                rgba: (0.05, 0.35, 0.35, 1)
                            Rectangle:
                                pos: self.pos
                                size: self.size
                        Label:
                            id: lbl_chat_log
                            text: "System: Sẵn sàng...\n"
                            size_hint_y: None
                            height: self.texture_size[1]
                            text_size: self.width, None
                            halign: 'left'
                            valign: 'top'
                            padding: [10, 10]
                            markup: True

                    ModernButton:
                        id: btn_talk
                        text: "BẤM ĐỂ NÓI"
                        size_hint_y: 0.2
                        background_color: (0.25, 0.1, 0.35, 1)
                        on_release: root.start_intercom()

                    ModernButton:
                        text: "XEM LỊCH SỬ"
                        size_hint_y: 0.1
                        background_color: (1, 0.5, 0, 1)
                        on_release: root.show_gallery()

                    # NÚT MỞ CÀI ĐẶT
                    ModernButton:
                        text: "CÀI ĐẶT HỆ THỐNG"
                        size_hint_y: 0.1
                        background_color: (0.4, 0.4, 0.4, 1)
                        on_release: root.toggle_settings_menu()

        # -----------------------------------------------------------
        # LỚP PHỦ MENU CÀI ĐẶT (Mặc định ẩn)
        # -----------------------------------------------------------
        BoxLayout:
            id: settings_menu
            orientation: 'vertical'
            size_hint: (0.4, 0.6)
            pos_hint: {'right': -1, 'center_y': 0.5} # Nằm ngoài màn hình bên trái
            padding: 15
            spacing: 15
            canvas.before:
                Color:
                    rgba: (0.1, 0.1, 0.2, 0.95)
                RoundedRectangle:
                    pos: self.pos
                    size: self.size
                    radius: [20, 0, 0, 20]
                Color:
                    rgba: c_primary
                Line:
                    rounded_rectangle: (self.x, self.y, self.width, self.height, 20)
                    width: 2

            Label:
                text: "CẤU HÌNH CAMERA"
                bold: True
                size_hint_y: 0.1
                color: c_primary

            # Phần Đổi IP
            BoxLayout:
                orientation: 'horizontal'
                size_hint_y: 0.15
                spacing: 5
                TextInput:
                    id: txt_ip_suffix
                    hint_text: "Địa chỉ IP"
                    multiline: False
                    size_hint_x: 0.7
                ModernButton:
                    text: "ĐỔI"
                    size_hint_x: 0.3
                    on_release: root.update_camera_ip(txt_ip_suffix.text)

            ModernButton:
                text: "DÒ IP TỰ ĐỘNG (MAC)"
                size_hint_y: 0.15
                background_color: (0, 0.8, 0.4, 1)
                on_release: root.auto_fix_camera_connection()

            Separator: # Một đường kẻ ngang giả
                size_hint_y: None
                height: 2

            Label:
                text: "ĐĂNG KÝ NGƯỜI NHÀ"
                bold: True
                size_hint_y: 0.1

            TextInput:
                id: txt_member_name
                hint_text: "Nhập tên người cần lưu..."
                size_hint_y: 0.15
                multiline: False

            BoxLayout:
                size_hint_y: 0.15
                spacing: 10
                ModernButton:
                    text: "XÓA"
                    background_color: (0.7, 0.2, 0.2, 1)
                    on_release: txt_member_name.text = ""
                ModernButton:
                    text: "LƯU MẶT"
                    background_color: (0.25, 0.1, 0.35, 1)
                    on_release: root.register_member()

            ModernButton:
                text: "ĐÓNG CÀI ĐẶT"
                size_hint_y: 0.1
                background_color: (0.5, 0.5, 0.5, 1)
                on_release: root.toggle_settings_menu()
<TutorScreen>:
    name: 'tutor'
    canvas.before:
        Color:
            rgba: c_bg
        Rectangle:
            pos: self.pos
            size: self.size

    BoxLayout:
        orientation: 'vertical'
        padding: 20
        spacing: 10

        # --- HEADER & NÚT BACK ---
        BoxLayout:
            size_hint_y: 0.1
            Button:
                text: "HOME"
                size_hint_x: 0.2
                background_color: (0.3, 0.3, 0.3, 1)
                on_release: root.go_back()
            Label:
                text: "ENGLISH TUTOR (A1 - C2)"
                bold: True
                font_size: '24sp'
                color: c_primary

        # --- THANH CHỌN LEVEL (GRID BUTTONS) ---
        GridLayout:
            cols: 6
            size_hint_y: 0.1
            spacing: 5
            
            Button:
                text: "A1"
                background_color: (0, 0.8, 0, 1) # Xanh lá
                on_release: root.select_level("A1")
            Button:
                text: "A2"
                background_color: (0.2, 0.8, 0, 1)
                on_release: root.select_level("A2")
            Button:
                text: "B1"
                background_color: (0.8, 0.8, 0, 1) # Vàng
                on_release: root.select_level("B1")
            Button:
                text: "B2"
                background_color: (1, 0.6, 0, 1) # Cam
                on_release: root.select_level("B2")
            Button:
                text: "C1"
                background_color: (1, 0, 0, 1) # Đỏ
                on_release: root.select_level("C1")
            Button:
                text: "C2"
                background_color: (0.5, 0, 0.5, 1) # Tím
                on_release: root.select_level("C2")

        # --- KHUNG HIỂN THỊ CHAT ---
        BoxLayout:
            size_hint_y: 0.6
            canvas.before:
                Color:
                    rgba: (0.1, 0.1, 0.15, 1)
                RoundedRectangle:
                    pos: self.pos
                    size: self.size
                    radius: [15,]
            
            ScrollView:
                padding: 10
                Label:
                    id: lbl_tutor_log
                    text: "Hãy chọn cấp độ để bắt đầu...\n"
                    size_hint_y: None
                    height: self.texture_size[1]
                    text_size: self.width, None
                    halign: 'left'
                    valign: 'top'
                    markup: True
                    font_size: '18sp'
                    padding: [20, 20]

        # --- NÚT BẤM NÓI ---
        ModernButton:
            text: "🎙️ BẤM ĐỂ TRÒ CHUYỆN"
            size_hint_y: 0.2
            background_color: c_primary
            on_release: root.start_voice_chat()

<ElderlyScreen>:
    name: 'elderly'
    BoxLayout:
        orientation: 'vertical'
        padding: 20
        spacing: 15
        
        BoxLayout:
            size_hint_y: 0.1
            spacing: 10
            Button:
                text: "< QUAY LẠI"
                on_release: root.go_back()
            
            # --- NÚT CHỈNH CỠ CHỮ ---
            Button:
                text: "Chữ A-"
                size_hint_x: 0.2
                on_release: root.adjust_font(-2)
            Button:
                text: "Chữ A+"
                size_hint_x: 0.2
                on_release: root.adjust_font(2)

        Label:
            text: "NGƯỜI BẠN TÂM GIAO"
            font_size: '28sp'
            bold: True
            size_hint_y: 0.1

        ScrollView:
            canvas.before:
                Color:
                    rgba: (0.1, 0.1, 0.15, 1)
                Rectangle:
                    pos: self.pos
                    size: self.size
            Label:
                id: lbl_elderly_log
                text: "" 
                font_size: '18sp' # Kích cỡ ban đầu 
                size_hint_y: None
                height: self.texture_size[1] 
                text_size: self.width, None
                markup: True 
                padding: [20, 20]

        ModernButton:
            id: btn_talk
            text: "🎙️ BẤM ĐỂ NÓI CHUYỆN"
            size_hint_y: 0.2
            on_release: root.toggle_voice_chat()